name: doctrine-encrypt-bundle
services:
  php:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: doctrine-encrypt-bundle-php
    working_dir: /app
    volumes:
      - .:/app
      - composer-cache:/root/.composer/cache
    environment:
      - COMPOSER_ALLOW_SUPERUSER=1
    command: ["tail", "-f", "/dev/null"]
    networks:
      - doctrine-encrypt-bundle-network

volumes:
  composer-cache:
    driver: local

networks:
  doctrine-encrypt-bundle-network:
    driver: bridge
